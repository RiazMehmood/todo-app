# Redpanda configuration for Minikube
statefulset:
  replicas: 1

resources:
  cpu:
    cores: "1000m"
  memory:
    container:
      max: "2Gi"
      min: "1Gi"

storage:
  persistentVolume:
    enabled: true
    size: 10Gi

external:
  enabled: false

auth:
  sasl:
    enabled: false
